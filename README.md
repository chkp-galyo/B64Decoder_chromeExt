# B64Decoder_chromeExt
A simple chrome extension for fast Base64 decoding.

Simply select the encoded text -> right click -> Decode base64.

The decoded text should be appended to the original encoded text.
